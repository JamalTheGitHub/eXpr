<html>
<body>
<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.5.0/css/all.css" integrity="sha384-B4dIYHKNBt8Bc12p+WXckhzcICo0wtJAoU8YZTY5qE0Id1GSseTk6S+L3BlXeVIU" crossorigin="anonymous">

<div class="card-deck">

  <% if signed_in? %>
  <div class="card">
  <%= button_to "Add new item?", new_user_grocery_path(current_user), method: :get,class:"btn btn-outline-primary btn-lg col-12 text-center" %>
  </div>
  <% end %>

<div class="card">
  <div class="box">
  <h1 style="font-size:8vw">Today</h1>
  <p><i class="fas fa-cookie" style="font-size:22px"></i>   All item total:<%= " #{@groceries.count}" %></p>
  <p><i class="fas fa-cookie-bite" style="font-size:22px"></i>   Expiring total:<%= " #{@exps.count}" %></p>
  <p><i class="fas fa-skull-crossbones" style="font-size:22px"></i>   Expired total: <%= " #{@expired.count}" %></p>
  </div>
</div>
  <div class="card">
    <div class="box">
    <div class="button"><%= button_to "All Items", user_groceries_path(current_user), type:"button", class:"btn btn-primary btn-block", method: :get %></div>
    <div class="button"><%= button_to "Expiring", user_expiries_path(current_user), type:"button", class:"btn btn-warning btn-block", method: :get %></div>
    <div id="button1"><%= button_to "Expired", user_expired_path(current_user), type:"button", class:"btn btn-danger btn-block", method: :get %></div>
    </div>
  </div>
</div>
</body>
</html>


<style>
h1 {
  font-family: "Comic Sans MS", cursive, sans-serif;
  text-align: center;
}
.button {
  margin-bottom:10px;
}
#button1 {
  margin:0;
}
.card {   
    top: 3vh;
    margin: 0 auto;
    width:350px;
    min-height:5px;
    background:#fff;
    box-shadow:0 20px 50px rgba(0,0,0,.1);
    border-radius:10px;
    transition:0.5s;
}

.box {
    margin: 20px;
}
h5 {
  margin: 2px;
  color: white;
}
#black {
  color:black;
}
</style>